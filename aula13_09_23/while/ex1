/* Escreva um algoritmo que mostre os números de 1 até 10, um número por linha */

#include <stdio.h>

int main() {
  
    int num = 0;
 
    while(num <= 9){
        printf("%d \n", num = num + 1);
    }
 
    return 0;
}
